{
  "name": "first-lunar-credit-union",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "yarn workspace frontend dev",
    "api:dev": "yarn workspace backend dev",
    "dev:all": "concurrently \"yarn dev\" \"yarn api:dev\"",
    "build": "yarn workspace frontend build",
    "build:dev": "yarn workspace frontend build:dev",
    "api:build": "yarn workspace backend build",
    "build:all": "yarn workspace frontend build && yarn workspace backend build",
    "lint": "yarn workspaces run lint",
    "preview": "yarn workspace frontend preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
